#include <iostream>

int main()
{
	/*
	배열: 같은 타입의 변수 여러개를 한번에 선언할 수 있는 기능.
	배열은 연속된 메모리 공간에 만들어진다.
	즉, 하나의 큰 메모리 공간을 할당하고 그 공간을 개수만큼 쪼개서 사용하게된다.

	타입 Array [개수];

	배열 개수를 지정할때는 반드시 상수로 지정해야한다.
	*/
	int Count = 100;

	/*
	배열의 각 요소에 접근하기 위해서 인덱스를 제공한다.
	인덱스는 0부터 개수 -1 까지 존재한다.

	배열 선언시 = {} 을 해주면 배열의 모든 요소를 0으로 초기화 해준다.
	= { 1, 2, 3, 4, 5 } 이렇게 해주면 0, 1, 2, 3, 4 번 인덱스에 1, 2, 3, 4, 5 의 값이 들어가고
	나머지는 초기화 해준다.

	배열의 오버플로우
	예를 들어 99번까진데 100번에 접근하려한다거나...
	그럼 안된다 죽음, if문으로 제한을 걸어두거나 해야함.

	배열은 for문이랑도 많이 사용한다.
	*/
	int Array[100]{ 1, 2, 3, 4, 5 };

	for (int i = 0; i < 100; ++i)
	{
		Array[i] = i + 1;
	}

	int Index = 79;

	if (Index < 0)
		Index = 0;

	else if (Index >= 100)
		Index = 99;

	Array[Index] = 3030;

	std::cout << Array[0] << std::endl;

	return 0;
}
